import type { Metadata } from 'next'
export { default } from './_client'

export const metadata: Metadata = {
  title: 'HTML Minifier & Compressor',
  description: 'Minify HTML online — strip comments, remove whitespace, and compress HTML code. Free HTML minifier for faster page loads. No server upload.',
  openGraph: {
    title: 'HTML Minifier & Compressor',
    description: 'Minify HTML online — strip comments, remove whitespace, and compress HTML code. Free HTML minifier for faster page loads. No server upload.',
  },
}
