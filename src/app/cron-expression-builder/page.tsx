import type { Metadata } from 'next'
export { default } from './_client'

export const metadata: Metadata = {
  title: 'Cron Expression Builder',
  description: 'Build cron expressions visually with human-readable translation. Free online cron scheduler with presets and field selectors for developers.',
  openGraph: {
    title: 'Cron Expression Builder',
    description: 'Build cron expressions visually with human-readable translation. Free online cron scheduler with presets and field selectors for developers.',
  },
}
