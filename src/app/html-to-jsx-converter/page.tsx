import type { Metadata } from 'next'
export { default } from './_client'

export const metadata: Metadata = {
  title: 'HTML to JSX Converter',
  description: 'Convert HTML to JSX online. Transform HTML tags to React-compatible JSX syntax — className, htmlFor, camelCase attributes, self-closing tags. Free converter.',
  openGraph: {
    title: 'HTML to JSX Converter',
    description: 'Convert HTML to JSX online. Transform HTML tags to React-compatible JSX syntax — className, htmlFor, camelCase attributes, self-closing tags. Free converter.',
  },
}
