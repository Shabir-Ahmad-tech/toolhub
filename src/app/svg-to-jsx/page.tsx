import type { Metadata } from 'next'

export const metadata: Metadata = {
  title: 'SVG to React/JSX Converter — Free Online Tool',
  description: 'Convert SVG markup to React JSX component syntax. Handles attributes, camelCase conversion, and inline style objects for React, Preact, and Vue components.',
}

export { default } from './_client'
